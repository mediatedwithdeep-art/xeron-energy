# Public site URL (drives canonical URLs, sitemap, JSON-LD schema)
NEXT_PUBLIC_SITE_URL=https://xeronenergy.in

# Optional: wire the contact API (src/app/api/contact/route.ts) to a real destination
# CONTACT_WEBHOOK_URL=
# WHATSAPP_BUSINESS_TOKEN=
# SMTP_HOST=
# SMTP_USER=
# SMTP_PASS=
